# Inherit base production dependencies
-r requirements.txt

# Testing Frameworks
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.7
respx>=0.21.1

# Linting, Formatting & Type Checking
ruff==0.15.14
black==26.5.1
mypy>=1.10.0
pre-commit>=3.7.0

# Security Scanning
pip-audit>=2.7.0
bandit>=1.7.8

# Phase 6: Offline Evaluation & Monitoring
# Ragas 0.1.x imports the legacy LangChain 0.2 stack. Keep it out of production
# service images and install it only for offline evaluation/CI quality gates.
langchain>=0.2.14,<0.3.0
langchain-core>=0.2.34,<0.3.0
langchain-openai>=0.1.22,<0.2.0
langchain-community>=0.2.12,<0.3.0
ragas==0.1.21
datasets>=2.20.0,<3.0.0
schedule>=1.2.1
