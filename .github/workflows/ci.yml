name: CI Pipeline

on:
  pull_request:
    branches: [main]

# Required permission for actions/github-script to post Ragas results to the PR
permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    name: Run Unit & Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          
      - name: Lint code
        run: |
          black --check src/
          ruff check src/
          
      - name: Security scan
        run: |
          pip install bandit
          bandit -r src/ -f json -o bandit-report.json || true
          
      - name: Run unit tests
        run: pytest tests/unit/ -v --cov=src --cov-report=term-missing --cov-fail-under=50
        
      - name: Run integration tests
        run: pytest tests/integration/ -v

  ragas-eval:
    name: LLM Quality Gate (Ragas)
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          
      # PRESERVED FROM PHASE 8: Local Fallback LLM Infrastructure
      - name: Install and Start Ollama (Local Fallback LLM)
        run: |
          curl -fsSL https://ollama.com/install.sh | sh
          ollama serve &
          sleep 5
          ollama pull llama3.1:8b-instruct
          
      # PRESERVED FROM PHASE 8: Core Backing Services
      - name: Start Background Services
        run: |
          docker-compose up -d db qdrant
          sleep 30
          
      # PRESERVED FROM PHASE 8: Spin up the API for E2E testing
      - name: Start API Server
        env:
          # Inject production API key for primary routing in testing
          LLM_OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }} 
        run: |
          uvicorn src.llm_orchestrator.main:app --host 0.0.0.0 --port 8000 &
          sleep 10
          
      # PHASE 9: Offline Evaluation Generation
      - name: Run Ragas Offline Evaluation (API Compromise Path)
        env:
          # Inject key for the Ragas evaluator models
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python src/evaluation/offline/ragas_eval.py
        
      # PHASE 9: Quality Enforcement Check
      - name: Check quality thresholds
        run: python scripts/check_thresholds.py
        
      # PHASE 9: Automated PR Reporting
      - name: Post results to PR
        uses: actions/github-script@v6
        if: always() # Ensures the comment posts even if thresholds fail
        with:
          script: |
            const fs = require('fs');
            if (fs.existsSync('ragas_results.json')) {
              const results = fs.readFileSync('ragas_results.json', 'utf8');
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: `## 📊 Ragas Evaluation Results\n\`\`\`json\n${results}\n\`\`\`\n*Automated quality gate generated by CI.*`
              });
            } else {
              console.log('ragas_results.json not found. Skipping PR comment.');
            }