# --- RAG service runtime deps (Phase 3 retrieval API) ---

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
loguru>=0.7.2
prometheus-client>=0.20.0
pandas>=2.0.0,<3.0.0

python-dotenv>=1.0.1
httpx>=0.27.0
orjson>=3.10.0
tenacity>=8.2.3

# Vector DB
qdrant-client>=1.10.1

# Retrieval
# FIXED: Prevent NumPy 2.x from causing C-extension crashes in rank-bm25/torch
numpy>=1.26.0,<2.0.0
rank-bm25>=0.2.2

# Chunking / text split
langchain-text-splitters>=0.2.2

# Local Embeddings/Reranker core dependencies
# PRODUCTION FIX: Removed <3.0.0 and <4.40 bounds to resolve tokenizers conflict
sentence-transformers>=2.5.0
transformers>=4.38.0
tokenizers>=0.15.0

# PRODUCTION FIX: Inject explicitly requested LangChain integration packages 
# into the RAG container boundary to support local HuggingFace routing
langchain-core>=0.2.34
langchain-huggingface>=0.0.3

# Postgres + text-to-SQL safety (needed by your safe_sql / historical chain)
sqlalchemy>=2.0.30
psycopg[binary]>=3.1.19
asyncpg>=0.29.0
alembic>=1.13.1
sqlglot>=25.10.0

# Ingestion-only (keep only if rag-service container also ingests PDFs)
pymupdf>=1.24.9

# CLI helpers (keep only if you run ingestion CLI inside this container)
typer>=0.12.3